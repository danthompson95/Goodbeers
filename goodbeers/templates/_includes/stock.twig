<div class="row">
	{% for post in posts %}
		<div class="col-md-4 mb-4">
			<div class="card">
				<a href="{{post.url}}">
					<h5 class="card-header">{{post.title}}</h5>
				</a>

			</div>
		</div>
	{% endfor %}
</div>
